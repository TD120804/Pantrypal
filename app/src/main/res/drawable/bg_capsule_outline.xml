<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Transparent fill -->
    <solid android:color="@android:color/transparent"/>

    <!-- Border -->
    <stroke
        android:width="1.5dp"
        android:color="#D6B25E"/>

    <!-- Pill shape -->
    <corners android:radius="50dp"/>

</shape>